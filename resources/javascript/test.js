const howOld = (age, year) => { // 25, 2030 / 25, 2020 / 25, 1990
    const currentYear = 2022
    // calculatedAge podaje ile lat osoba ma w roku year
    const calculatedAge = year - currentYear + age // 33 / 23 / -7

    if (calculatedAge > age) {}
    if else (calculatedAge < 0) {}
    else { return ``}
}